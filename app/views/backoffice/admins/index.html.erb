<h1 class="page-header">Administradores</h1>
<div class="panel panel-default">
  <div class="panel-heading">
    Lista de Administradores
    <%= link_to new_backoffice_admin_path, class: "btn btn-primary btn-circle pull-right", style: 'margin-top: -5px;', title: 'Novo' do %>
      <i class="fa fa-plus"></i>
    <% end %>
  </div>
  <!-- /.panel-heading -->
  <div class="panel-body">
    <div class="table-responsive">
      <table class="table table-striped table-bordered table-hover">
        <thead>
        <tr>
          <th>#</th>
          <th>E-mail</th>
          <th style="width: 8%">Ações</th>
        </tr>
        </thead>
        <tbody>
        <% @admin.each do |admin| %>
          <tr>
            <td><%= admin.id %></td>
            <td><%= admin.email %></td>
            <td style="text-align: center;">
              <%= link_to edit_backoffice_admin_path(admin.id), class: "btn btn-primary btn-circle", title: 'Editar' do %>
                <i class="fa fa-edit"></i>
              <% end %>
              <% if current_admin != @admin.id %>
                <%= link_to backoffice_admin_path(admin.id), method: :delete, class: "btn btn-danger btn-circle", title: 'Editar', data: {confirm: "Deseja realmente excluir o admin #{admin.email}?"} do %>
                  <i class="fa fa-remove"></i>
                <% end %>
              <% end %>

            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
    <!-- /.table-responsive -->
  </div>
  <!-- /.panel-body -->
</div>
